import 'package:flutter/material.dart';

class CovidResource extends StatelessWidget {
  const CovidResource({Key? key}) : super(key: key);
  static const routeName = '/CovidResource';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
